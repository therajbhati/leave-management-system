export const environment = {
  production: true,
  apiUrl: 'https://leave-management-system-glxq.onrender.com/api',
  // Must exactly match ENCRYPTION_KEY in backend .env (use a different strong key in prod)
  encryptionKey: 'your_shared_encryption_key_here',
};
